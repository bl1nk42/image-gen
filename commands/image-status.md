---
description: Show image cache statistics, backend health, and recent queue state
argument-hint: 
---
Call `cache_stats`, check the Modal health endpoint, and list recent job IDs if available. Do not clear cache or cancel jobs from this read-only command.
