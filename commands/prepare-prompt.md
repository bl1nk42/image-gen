---
description: Preview normalized prompt, style suffix, and negative prompt before generation
argument-hint: <prompt> [style]
---
Use the plugin's `prepare_prompt_tool` with the user's prompt and optional style. Show the executed prompt, negative prompt, and normalization operations. Do not generate an image in this command.
