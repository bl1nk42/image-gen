# Memory Contract

A memory result contains `key_information`, `source`, `date`, `confidence`, `current_state_check`, and `recommended_use`. Confidence is high only when multiple consistent sources or a current file confirms it. Mark contradictions explicitly. For image style memory, track sample count, up/down votes, negative concepts, and whether the sample was human-approved.
