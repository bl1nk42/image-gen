# Nothing-inspired Tokens

```css
:root { --bg-dark:#0a0a0a; --bg-light:#f4f3ef; --text-display:#fff; --text-primary:#e8e8e8; --text-secondary:#999; --text-disabled:#666; --interrupt:#d71921; --grid:4px; --radius-technical:6px; }
```

Dark and light are separate themes, not inverted copies. Use 4/8/16/32/64px spacing rhythm. Load `Space Grotesk` and `Space Mono` from Google Fonts only when external fonts are allowed; otherwise use local fallback and disclose it. Use red only for an interrupt or status. Favor exposed grids, labels, mono metrics, flat surfaces, and one asymmetric focal moment.
